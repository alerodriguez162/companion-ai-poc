{
  "buildCommand": "pnpm --filter @companion/shared build && pnpm --filter @companion/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
